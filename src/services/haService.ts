export const haService = {
  async callService(
    _domain: string,
    _service: string,
    _data?: Record<string, unknown>,
  ): Promise<void> {
    // Stub: no Home Assistant backend wired up yet.
    return
  },
}